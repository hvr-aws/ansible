- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: install inginx
    ansible.builtin.package:
    name: nginx
    state: present


  - name: run nginx
    ansible.builtin.service:
    name: nginx
    state: present
    enable: yes

